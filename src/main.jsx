import ReactDOM from 'react-dom';
import App from './App';

const root = document.getElementById('root');

ReactDOM.render(<App />, root);
// ReactDOM.createRoot(root).render(<App />); // страница пустой
